services:
  proxy:
    image: nginx:latest
    container_name: group_proxy
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - group_app
      - group_admin
    networks:
      - secure-net